export interface Comment {
  timestamp: string;
  userComment: string;
  userEmail: string;
  userId: string;
  userName: string;
}
export interface Post {
  email: string;
  firstName: string;
  image: string;
  text: string;
  timestamp: string;
  uid: string;
}
